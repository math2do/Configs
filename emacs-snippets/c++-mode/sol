# -*- mode: snippet -*-
# name: sol
# key: sol	
# --

/**
 *    author:  math2do
 *    created: `(format-time-string "%Y-%m-%d %H:%M:%S")`
**/

#include <bits/stdc++.h>

using namespace std;

#ifdef LOCAL
#include "lib/debug.h"
#else
#define debug(...) 42
#endif

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  $0
  return 0;
}